#include "main.h"
/**
 * main - check the code.
 * print_sign - function for greater than
 * Return: Always 0.
 * @n: the int to be checked
 */

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
	]
	return (1);
	_putchar('\n');

	else if (n == 0)
	{
		_putchar(48);
	}
	return (0);
	_putchar('\n');

	else
	{
		_putchar('-');
	}
}
