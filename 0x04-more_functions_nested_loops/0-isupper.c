#include <stdio.h>
#include "main.h"

/**
 *_isupper - This function differenciates
 * @c:returns 1 if isupper
 * Return: either 1 or 0
 */

int _isupper(int c)
{
	if (isupper(c));
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

